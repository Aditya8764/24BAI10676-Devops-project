apiVersion: v1
kind: Service
metadata:
  name: event-website-service
spec:
  type: NodePort
  selector:
    app: event-website
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080 # Website will be accessible at http://<Node-IP>:30080